# Hoist Expo and React Native deps so Metro can resolve them through the
# nested node_modules layout pnpm produces. Without this, RN's autolinker
# misses packages that live in the workspace root.
node-linker=hoisted
shamefully-hoist=true
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*eslint*
